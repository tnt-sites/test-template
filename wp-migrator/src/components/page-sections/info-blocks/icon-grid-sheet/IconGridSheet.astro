---
import CustomSection from "@builders/custom-section/CustomSection.astro";
import Heading from "@core-elements/heading/Heading.astro";
import Icon from "@core-elements/icon/Icon.astro";

const { heading = "", backgroundColor = "base", backgroundGradient = "", backgroundImage,
  class: className, ...rest } = Astro.props;

const htmlAttributes = Object.fromEntries(
  Object.entries(rest).filter(([key]) => key !== "_component")
);

const iconFiles = Object.keys(import.meta.glob("/src/icons/**/*.svg"));

const iconItems = iconFiles
  .map((filePath) => {
    const relativePath = filePath.replace("/src/icons/", "");
    const iconName = relativePath.replace(/\.svg$/i, "");
    const fileName =
      relativePath
        .split("/")
        .pop()
        ?.replace(/\.svg$/i, "") ?? iconName;
    return { iconName, relativePath, fileName };
  })
  .sort((a, b) => a.relativePath.localeCompare(b.relativePath));
---

<CustomSection
  class:list={["icon-grid-sheet", className]}
  maxContentWidth="2xl"
  paddingHorizontal="lg"
  paddingVertical="4xl"
  colorScheme="default"
  backgroundColor={backgroundColor}
  backgroundGradient={backgroundGradient}
  backgroundImage={backgroundImage}
  {...htmlAttributes}
>
  <Heading level="h2" size="lg" alignX="center" data-prop="heading" text={heading} />

  <div>
    {
      iconItems.length > 0 ? (
        <ul class="icon-grid-sheet-grid" aria-label="Available icons">
          {iconItems.map((item) => (
            <li class="icon-grid-sheet-item">
              <div class="icon-grid-sheet-preview" aria-hidden="true">
                <Icon name={item.iconName} size="lg" color="default" />
              </div>
              <p class="icon-grid-sheet-filename">{item.fileName}</p>
              {item.relativePath !== `${item.fileName}.svg` && (
                <p class="icon-grid-sheet-path">{item.relativePath}</p>
              )}
            </li>
          ))}
        </ul>
      ) : (
        <p class="icon-grid-sheet-empty">No icons found in src/icons.</p>
      )
    }
  </div>
</CustomSection>

<style lang="pcss" is:global>
  .icon-grid-sheet {
    .icon-grid-sheet-grid {
      display: flex;
      align-items: start;
      justify-content: center;
      flex-wrap: wrap;
      max-width: 1200px;
      margin: 50px auto;
      gap:10px;
    }

    .icon-grid-sheet-item {
      background-color: var(--color-bg-surface);
      border-radius: 5px;
      display: grid;
      gap: 10px;
      justify-items: center;
      padding: 20px;
      text-align: center;
    }

    .icon-grid-sheet-preview {
      align-items: center;
      display: flex;
      height: 52px;
      justify-content: center;
      width: 52px;
    }

    .icon-grid-sheet-filename,
    .icon-grid-sheet-path {
      margin: 0;
    }

  }
</style>
