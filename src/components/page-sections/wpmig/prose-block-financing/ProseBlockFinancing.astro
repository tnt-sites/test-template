---
/**
 * Prose Block Financing — migrated from new-patients [4] (auto) by wp-migrator.
 *
 * Baked in: layout, spacing, typography. Props: text, images, links, colours.
 */
const {
  heading = "",
  text = "",
  subheading = "",
  text2 = "",
  backgroundColor = "",
  backgroundColorHex = "var(--color-brand)",
  headingColor = "#ffffff",
  headingColorHex = "",
  textColor = "#ffffff",
  textColorHex = "",
  id,
  _component,
  ...rest
} = Astro.props;
const backgroundColorValue = backgroundColorHex || backgroundColor || "var(--color-brand)";
const headingColorValue = headingColorHex || headingColor || "#ffffff";
const textColorValue = textColorHex || textColor || "#ffffff";
---
<div class="prose-block-financing" id={id || undefined} style={`--bg:${backgroundColorValue};--hd:${headingColorValue};--tx:${textColorValue}`} {...rest}>
  <div class="pbf-box">
    <div class="pbf-box2">
      <div class="pbf-box3">
        <h2 class="pbf-heading">{heading}</h2>
      </div>
      <div class="pbf-box4">
        <div class="pbf-box5">
          {text && <p class="pbf-text">{text}</p>}
        </div>
      </div>
      <div class="pbf-box6">
        <hr class="pbf-box7" />
      </div>
      <div class="pbf-box8">
        {subheading && <h3 class="pbf-heading2">{subheading}</h3>}
      </div>
      <div class="pbf-box9">
        {text2 && <div class="pbf-text2" set:html={text2} />}
      </div>
    </div>
  </div>
</div>
<style>
  .prose-block-financing {
    background-color: var(--bg);
    box-sizing: border-box;
    color: #333333;
    display: flex;
    flex-direction: column;
    font-family: Lato, sans-serif;
    font-size: 18px;
    line-height: 27px;
    max-width: 100%;
    padding-left: 10px;
    padding-right: 10px;
    position: relative;
  }

  .pbf-box {
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    flex-grow: 1;
    flex-wrap: wrap;
    gap: 20px;
    margin-left: auto;
    margin-right: auto;
    max-width: min(100%, 767px);
    width: 100%;
    padding-bottom: 70px;
    padding-top: 70px;
  }

  .pbf-box2 {
    align-items: center;
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
    gap: 20px;
    justify-content: center;
    position: relative;
  }

  .pbf-box3 {
    box-sizing: border-box;
    margin-left: auto;
    margin-right: auto;
    max-width: none;
    text-align: center;
  }

  .pbf-heading {
    box-sizing: border-box;
    color: var(--hd);
    font-family: "Playfair Display", sans-serif;
    font-size: 28px;
    font-weight: 700;
    line-height: 28px;
    margin-bottom: 0px;
    margin-top: 0px;
  }

  .pbf-box4 {
    box-sizing: border-box;
    color: #ffffff;
    margin-left: auto;
    margin-right: auto;
    max-width: min(100%, 840px);
    position: relative;
    text-align: center;
  }

  .pbf-box5 {
    box-sizing: border-box;
  }

  .pbf-text {
    box-sizing: border-box;
    margin-bottom: 14.4px;
    margin-top: 0px;
  }

  .pbf-box6 {
    align-self: stretch;
    box-sizing: border-box;
    display: block;
    padding-bottom: 15px;
    padding-top: 15px;
    text-align: center;
  }

  .pbf-box7 {
    border: 0;
    border-top: 1px solid #f4f8f7;
    box-sizing: border-box;
    margin-left: auto;
    margin-right: auto;
    max-width: 90%;
    width: 700px;
  }

  .pbf-box8 {
    box-sizing: border-box;
    margin-left: auto;
    margin-right: auto;
    max-width: 217px;
    text-align: center;
  }

  .pbf-heading2 {
    box-sizing: border-box;
    color: #ffffff;
    font-family: "Playfair Display", sans-serif;
    font-size: 22px;
    font-style: italic;
    font-weight: 700;
    line-height: 22px;
    margin-bottom: 0px;
    margin-top: 0px;
  }

  .pbf-box9 {
    box-sizing: border-box;
    color: #ffffff;
    max-width: 100%;
    position: relative;
    text-align: center;
  }

  .pbf-text2 {
    box-sizing: border-box;
  }

  @media (min-width: 768px) {
    .pbf-box {
      flex-wrap: nowrap;
      max-width: min(100%, 1024px);
    }

    .pbf-box2 {
      flex-wrap: nowrap;
      padding: 10px;
    }

    .pbf-heading {
      font-size: 32px;
      line-height: 32px;
    }

    .pbf-box8 {
      max-width: 247px;
    }

    .pbf-heading2 {
      font-size: 25px;
      line-height: 25px;
    }
  }

  @media (min-width: 1440px) {
    .pbf-box {
      max-width: min(100%, 1440px);
    }

    .pbf-heading {
      font-size: 45px;
      line-height: 45px;
    }
  }
</style>
